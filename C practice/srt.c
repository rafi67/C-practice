#include <stdio.h>
#include "work.h"

void main() {
    double a,b,c;
    a = 2.5;
    b = 2.5;
    c = add(a,b);
    printf("%lf\n",c);
    result();
    sum(a,b);
    max_result();
    Test();
    sqrt();
}
